import React from 'react';

const ProductCard = ({ product }) => {
    return (
        <div>
            <h3>{product.roadbike}</h3>
            <h3>{product.Cervelo}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    );
    return (
    <div>
            <h3>{product.roadbike2}</h3>
            <h3>{product.Bianchi}</h3>
            <p>{product.escriptiond}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike3}</h3>
            <h3>{product.Canyon}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike4}</h3>
            <h3>{product.Orbea}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike5}</h3>
            <h3>{product.Pinarello}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.Mountain}</h3>
            <h3>{product.cervelo}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.Mountain}</h3>
            <h3>{product.Pinarello}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.Mountain}</h3>
            <p>{product.Orbea}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.Mountain}</h3>
            <p>{product.Sava}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.resetrepeat}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    );
    return (
    <div>
            <h3>{product.roadbike2}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike3}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike4}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike5}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike6}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike7}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike8}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike9}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    );
    return (
    <div>
            <h3>{product.roadbike2}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike3}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike4}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike5}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike6}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike7}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike8}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike9}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    );
    return (
    <div>
            <h3>{product.roadbike2}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike3}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike4}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike5}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike6}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike7}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike8}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )
    return (
        <div>
            <h3>{product.roadbike9}</h3>
            <p>{product.description}</p>
            <p>${product.price}</p>
            <img src={product.image} alt={product.roadbike} style={{ width: '100px' }} />
        </div>
    )

};

export default ProductCard;
